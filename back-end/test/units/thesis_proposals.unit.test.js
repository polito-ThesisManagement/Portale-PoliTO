require('jest');

afterEach(() => {
  jest.restoreAllMocks();
  jest.clearAllMocks();
});

describe('ThesisProposals', () => {
  test('getThesisProposals', () => {
    expect(true).toBe(true);
  });
});
